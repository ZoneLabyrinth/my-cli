The project is a simple cli project

# Usage
In the project directory, you can run:
### npm insatll
install dependencies for the project

### mycli -h
show mycli all commander

### mycli init
init a project with mycli
